普通模板：

```c++
template<typename T>
struct hash {
    size_t operator()(T const& value) const {
        // 泛型版本的哈希
    }
};
```

特化模板：

```c++
namespace std{
    template<>
struct hash<Vertex> {
    size_t operator()(Vertex const& vertex) const {
        // 专门为 Vertex 类型定制的哈希方式
    }
};
}
```

---